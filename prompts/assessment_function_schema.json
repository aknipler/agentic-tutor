{
    "name": "check_answer",
    "description": "Returns true or false if the answer is correct.",
    "strict": true,
    "parameters": {
      "type": "object",
      "required": [
        "correctness"
      ],
      "properties": {
        "correctness": {
          "type": "boolean",
          "description": "Indicates if the answer is correct (true) or not (false). Be fairly lenient, as the answer can be correct even if it's not exactly the same as the expected answer."
        }
      },
      "additionalProperties": false
    }
  }