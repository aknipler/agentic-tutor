- The tutor (Modules) is Socratic for 3 concepts, then mid-session will stop being Socratic. While in Socratic mode, performance is good and follow-up questions are seamless. Hints are sufficiently convincing. However, Socratic behaviour is lost at some point, and it starts behaving like traditional ChatGPT. It will go back to Socratic for the next prompt if asked (but will only last 1 prompt). It also randomly changes the answer structure. (Note: this feedback was given when project used gpt-4o for tutor and gpt-40-mini for assessor. It is unknown if this behaviour is model dependent or not.)
  - The original "Full Detailed Answer Mode" section let the model switch out of Socratic mode on its own judgement ("repeated guided attempts have failed and a complete worked example is educationally justified"), and the Default Response Structure had an escape hatch ("...unless another format is clearly more suitable"). Neither section told the model to snap back afterwards — so once it free-formed one answer, its own prior turn became the exemplar it continued from, and only an explicit ask would (briefly) restore Socratic mode. I rewrote both sections in prompts/tutor.md: the full-answer trigger now requires an explicit request only (stalled attempts get an offer, not an automatic switch), the format escape hatch is gone, and there's now a bolded reversion rule: return to the Default Response Structure immediately after a full answer, even mid-problem.
- Chats from previous sessions are not displayed after loading back in. (i.e. logout/login again)
  - Tutor Module did not load the persisted chat history. Implemented that if the chosen competency had pre-existing chat history, it will load it. 
  - Assessor was built to load it, but had a bug. 
- LaTex module does not work (equations are written in text, with latex syntax).
  - Prompts did not tell models to wrap equations in "$" for inline or "$$" for block maths. Adjusted prompts to include that. Streamlit renders latex default if wrapped this way.
- When I turn on “Answer by voice”, there is no record button. (Mac OS)
  - Known Mac OS issue with Streamlit. Just required updating Streamlit to a more recent version.
- Assessor questions get flagged as red, orange, green but competencies in the tutor (Module 1, Module 2 etc.) interface do not change colours. 
  - Database had correct truth, but streamlit never updated the cache. Fixed this.
- Crash on Windows console encoding (utils/tutor/interface.py:1-16) — the real reason "the bot output wasn't printed" persisted even after the vector store fix. 
  - Several print() debug statements echo raw model output (including the Unicode minus sign −, common in generated equations) straight to the console. Windows defaults stdout to the cp1252 codec, which can't encode that character, so print() raised UnicodeEncodeError mid-response — before the answer was ever added to chat history or rendered. I reconfigured stdout/stderr to UTF-8 with errors="replace" at module import time, so a debug print can never take down a real answer again.
- Missing bold/LaTeX formatting (prompts/tutor.md) — prompts/tutor.md already asked for **bold labels** and $...$ LaTeX, but gpt-5-mini was quietly dropping the literal markdown syntax while keeping the prose structure. 
  - I made both instructions explicit about needing the literal characters (not just the concept), and added worked examples. Verified in the browser: the model now emits real <strong> tags and $...$/$$...$$ math that renders through KaTeX correctly.
- Does the tutor reload chat history per competency/topic?
  - All messages for a module (across every topic discussed that session) live in one list, st.session_state["messages_module_{id}"]. The UI shows this whole list — that's why you see earlier topics' Q&A still sitting above the new one after a transition. But what the model actually receives is different: on every topic transition, TutorState.set_topic_cutoff_index() (utils/tutor/state.py:76) records the current message count as a cutoff. get_conversation_context() only sends messages after that cutoff, capped at the last 5 — so the model only ever sees the current topic's exchanges, never prior topics'. The opening question for each new topic is generated via a standalone API call (build_initial_topic_prompt, no previous_response_id) — so each topic starts the model with zero prior turns, not chained to the previous topic's conversation. Within a topic, turns are chained via previous_response_id for continuity. None of this persists across a logout/refresh except via mongodb/logger.py's conversation log, which is replayed (load_logged_conversation_messages) only for a topic that was already fully logged before.
- Can we prompt cache the tutor competency prompt so that it saves token usage and just append the relevant course index stuff.
  - It's already possible and free — OpenAI's Responses API caches automatically based on matching the identical leading prefix of instructions+input across calls, no manual index or cache key needed. But utils/tutor/interface.py:492-500 currently defeats it: it builds system_prompt by prepending the per-topic bits (Current Topic: ..., description, learning outcomes) before the static tutor.md content. Since prefix-caching only matches from byte 0, and the topic name changes every call, the cache never hits even though most of the ~1-2k token block is identical every time. Swapping the order — static tutor.md first, then a ## Current Topic section appended after lets the bulk of it get cached automatically and cut cost/latency on every turn.
- Tutor bot returns blank response. There are multiple issues that have already been fixed regarding this.
  - GPT-5-mini is a reasoning model. Before it writes the visible reply, it generates internal "reasoning tokens" that are billed as output tokens even though the student never sees them. Neither API call in interface.py sets a reasoning effort level or max_output_tokens, so the model is left to decide on its own how much to think. For this system prompt specifically (Socratic pedagogy rules + strict Markdown/LaTeX formatting + function-calling + Australian spelling, all at once), that's a lot to juggle, so it can burn a large chunk of its token budget reasoning about the reply before writing any of it. If it runs out mid-reasoning, you get: zero visible text, no function call, nothing. Not because the request was cheap, but because the "thinking" phase ate the whole allowance. To fix the source of the issue, pass reasoning={"effort": "low"} (or "minimal") and an explicit max_output_tokens on these calls, guaranteeing room is reserved for the actual reply.
- When submitting the answer for the question, it always shows "Error saving assessment results” message. (Note: I am not sure if this was for the login codes that did not have a user_progress created in the database.) (Mac OS)
  - This was probably due to user_module_progress not being created for the user. Also updated Streamlit to newest version though. Issue resolved.
- Module 1's title sometimes showed as Module 2's or Module 3's after visiting another module and clicking back, while its topics stayed correct.
  - Each module page defined its own `load_module_data()` under `@st.cache_data(ttl=10)`. Streamlit keys a cached function by (`__module__`, `__qualname__`, source text) and every page script runs as `__main__` — the three copies were byte-identical and took no arguments, so all three shared one cache entry (verified: the two functions hash to the same key). Whichever module page rendered last filled it, and the next page to load read that module's document until the 10s TTL expired. The topics looked right because `render_tutor_interface` ignores the `topics` it is passed and re-reads them by index; the title, description and — silently — the `vector_store_id` did not, so the tutor was also searching the wrong week's lecture material. Replaced with one `get_cached_module(module_id)` in utils/cache.py, keyed by the module id.
- Students who jump around a module ("can we do ANOVA instead?") were taught the right topic but tracked against the wrong one.
  - The tutor would follow the student's request in conversation, but nothing told the application, so the per-topic prompt, the learning outcomes injected into it, and every `update_topic_competency` write still pointed at the topic they had left — the topic they actually completed stayed red, and one they had abandoned went green. Added a `switch_topic(topic_name, reason)` tool: the name is resolved app-side against that module's own topic list (exact, normalised, containment, abbreviation such as "gauge r&r", then a strict close match), an unresolvable or ambiguous name returns the topic list so the tutor asks instead of guessing, and a resolved one moves the topic cutoff, sets the current topic and opens the new topic with a question built from its own description and learning outcomes. The module's topic list and each topic's status now go into the system prompt so the model can pass an exact name.
  - Also scoped the current topic per module (`current_topic_{module_id}`), which was the last piece of tutor state still shared across every module page — a switch made on one module no longer survives a visit to another only by luck of `ensure_current_topic_for_module` re-deriving it.
- A student's chosen topic didn't survive a logout, and returning to a topic restarted it.
  - Two gaps left by the switching work above. (a) Where a student is up to was derived entirely from competency levels, which record what they've finished but not what they chose to work on — so a student who jumped ahead was dropped back at the first unfinished topic on their next login. Added `last_topic` (and `last_topic_at`) to the module's entry in `user_module_progress`, written by `set_active_topic` on every topic change and read by `get_resume_topic` when a module page opens; it falls back to the first unfinished topic for records that predate it, for a topic that has since been renamed out of the module data, and for a finished module. The write is guarded on the module entry already existing so it can't mint a half-built record. (b) Arriving at a topic now goes through one path, `open_topic`, whichever way the student got there — finishing the previous topic, switching, or opening the page — and a topic that already has a logged conversation resumes from it instead of generating a fresh diagnostic question and asking them to prove themselves twice. Replayed messages are deliberately not re-logged (that would duplicate the topic's history on every visit) and force a rerun so `render_chat_history` draws the student's own turns with the right roles rather than inside an assistant bubble.
  - Fell out of the refactor: the opening question's response id now survives a topic transition, so the student's first reply chains from the question they were actually asked instead of re-sending a message window. Only the login path used to keep it.
- Found by driving the live app (PRQ26DT2YY), not by the offline tests — both of these only appear once a real student switches topics.
  - "Can we jump to X?" and the reply to it were logged under the topic being *left*, so they became its final exchange. Coming back to that topic replayed a transcript ending in "let's switch to somewhere else", and handed that to the model as the most recent thing said. The lead message's topic is now the payload's call (`lead_topic`): a congratulation closes out the topic it names, a switch request belongs to the topic being asked for. Confirmed in the log — the departure exchange now files under the destination and the topic left behind gains nothing.
  - Switching away could *lower* a topic's competency. Asked to leave, the model summarised the topic it was leaving with `update_topic_competency(0, "No substantive attempts...")` and took a real student's topic from level 1 back to 0 — erasing work they had done, and the ticks on the progress page are what students trust. `handle_competency_update` now refuses any level below what is already recorded and tells the model why; the prompt bullet that invited the call on the way out was reworded to permit it only for crediting work.
  - `get_bot_response`'s catch-all now prints the traceback, not just `str(e)`. It covers two API calls, the competency write, the topic change and the logging, and the message alone named none of them — diagnosing the SDK failure below meant reproducing the whole turn offline to find out where it came from.
- Upgraded `openai` 1.66.3 -> 2.7.2 (pyproject, requirements, uv.lock, venv), fixing the intermittent "Sorry, something went wrong on my end" turns.
  - 1.66.3 cached discriminated-union metadata by `setattr` on the `typing.Union` object, which Python 3.14 made immutable. Measured on this interpreter: 1.66.3 resolves 0 of 62 discriminated unions, every one raising; 2.7.2 resolves 157 and raises none, having moved to a module-level `DISCRIMINATOR_CACHE`. It presented as flaky because it only fires when a response's parsing actually reaches one of those unions.
  - Checked before upgrading, against a throwaway 2.7.2 environment: every SDK surface this repo uses is intact — `responses.create` (all eight keyword arguments, streamed and not, with function tools and `file_search`), `chat.completions.create` with `max_completion_tokens` + `reasoning_effort`, `audio.transcriptions.create`, `files.create`, the whole `vector_stores` surface including `file_batches.upload_and_poll`, `client.beta.assistants`, and `from openai.types.vector_store import VectorStore`. The one 2.0.0 breaking change widens `ResponseFunctionToolCallOutputItem.output`, which we only ever build as input and never read off a response.
  - Verified live on 2.7.2 (PRQ26DT2YY): tutor streamed a reply, called `update_topic_competency` at level 2, transitioned, and replayed the topic it wrapped back to — with the write confirmed in the database; assessor graded a tutorial question to level 2 with feedback, both by calling `assess_answer` directly and through the page, with the result saved.


